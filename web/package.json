{
  "name": "wavefield-web",
  "version": "1.0.0",
  "private": true,
  "description": "Browser heatmap visualization for 2D FDTD wavefield",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "test": "vitest run"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "vite": "^5.4.0",
    "vitest": "^2.1.0"
  }
}
